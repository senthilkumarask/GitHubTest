# @version $Id: //product/DAF/version/11.1/Endeca/Index/src/module/config/atg/endeca/index/DimensionDocumentSubmitter.properties#1 $$Change: 875535 $
$class=atg.endeca.index.RecordStoreDocumentSubmitter
$classloader=/atg/endeca/EndecaClassLoaderService
$description=A DocumentSubmitter that submits dimension nodes to a "dimvals" CAS RecordStore. 

# our data store type. 
endecaDataStoreType=typeahead-dimvals

# work around multi-parenting for the moment.
submitOnlyFirstValueForProperty=true

# If resetActive is true, then active sessions of RecordStoreSubmitter are reset
# in case of an exception when trying to start Endeca transaction 
# This may be necessary in case of an unfinished Endeca transaction
resetActive^=DataDocumentSubmitter.resetActive

# Whether to flush after every record. This can be useful for debugging
# when submitting a particular record to the record store is triggering
# and error.
# 
# flushAfterEveryRecord=true

# Enabled for debugging
#
# loggingDebug=true

# Whether we are enabled. Disabled will remake this 
# component report success, even when it didn't do anything.
# Only useful for debugging.
#
enabled^=DataDocumentSubmitter.enabled


# persist per id, so that "path" variants can be saved
persistVariantsPerRepositoryId=true

# persist variant suffixes so that we can delete them later.
persistVariantSuffixesDir={atg.dynamo.server.home}/data/idxvariants

# persist the "keys" (locales in our case), that we use
# to multiplex against data stores. This allows us to send deletes
# to all the actual datastores
persistKeysDir^=DataDocumentSubmitter.persistKeysDir

# The host and application configuration, from which we get our
# port and DataStore splitting strategy, if any
indexingApplicationConfiguration^=DataDocumentSubmitter.indexingApplicationConfiguration

#
# The ID property name. Here overriden to be dimval.spec. Probably
# Should eventually move this property to EndecaIndexingOutputConfig
# so it can be easily shared.
#
idPropertyName=dimval.qualified_spec


# Following settings for using the new-style CAS deployment tempate:
#
# everyRecordPropertyValues=\
#  Endeca.IsDimensionValueRecord=true
# 
# idPropertyName=Endeca.Id
# propertyNameReplacementMap=dimval.qualified_spec=Endeca.Id,\
#  dimval.dimension_spec=dimval.dimension_name
